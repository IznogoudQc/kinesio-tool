# 2026-07-05

Suite du polissage du rapport PDF (après la grosse refonte thématique de la veille).

## v0.1.59 — Espace constant avant « Ce que ça veut dire »

Le bloc d'interprétation paraissait « des fois collé » au graphique. Cause : il n'avait pas de marge haute
propre, il empruntait le `marginBottom` du dernier graphique — absent quand pas de graphique ou après une
coupure de page. Correctif : `marginTop: 12mm` explicite (les marges fusionnent → ~12 mm garanti partout).

## v0.1.60 — Section « Objectif du client »

Demande de Nicholas : ajouter l'objectif du client au rapport. Décidé ensemble :

- **Portée par bilan** (comme les notes) — l'objectif peut évoluer ; aucune migration DB (JSON `data`).
- **Emplacement : encadré « Votre objectif »** en tête de la Vue d'ensemble (Section 1), avant les scores —
  bandeau doré, citation en italique. Donne du sens aux scores (un 2,0 se lit selon ce qu'on visait).

Saisie via un textarea dans le groupe « Objectif, notes et observations » du formulaire (+ hint d'exemple).
Rendu conditionnel (masqué si vide). Validation IPC `max(2000)`.

## v0.1.61 — Objectif chiffré & module nutrition (opt-in par client)

Nicholas voulait un objectif **chiffré** : viser un % de gras → livres à perdre + macros. Décidé : macros **avec
avertissement** (champ de pratique OPDQ), cible = % de gras. ADR 0015.

- Moteur pur `src/lib/nutrition.ts` (12 tests) : `bodyFatGoal` (masse maigre préservée), `mifflinBmr`, `estimateMacros`.
- 3 colonnes `clients` (migration `0008`), config dans `EditClientModal`, rendu `ObjectifBlock` en Vue d'ensemble.
- Cas Nicholas : 99.8 kg / 30,2 % → cible 15 % = **~39 lb à perdre** (poids visé 181 lb), 2310 kcal / 164 P / 270 G / 64 L.
- 149 tests au vert, build OK.

## v0.1.62 — Rythme de perte paramétrable + échéance estimée

Suite directe : un rythme (Lent/Modéré/Soutenu/Rapide, kg/sem) choisi par client, qui pilote **à la fois** l'échéance
estimée (rapport) et le déficit calorique des macros (1 kg gras ≈ 7700 kcal). Colonne `nutrition_rate_kg_per_week`
(migration `0009`), `estimateMacros` accepte un `dailyDeficitKcal`. Cas Nicholas à 0,5 kg/sem → ~36 semaines (~8 mois),
déficit 550 kcal → 2338 kcal cible. 155 tests, build OK.

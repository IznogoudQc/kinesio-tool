# 2026-07-19

## v0.8.4 — Import .doc : hanche lue à 5 au lieu de 112

**Symptôme** (Marie / Nicholas) : à l'import du bilan `.doc` du logiciel d'origine, la **Hanche** apparaissait
à **5** dans Mesures (via la synchro bilan→mesures) au lieu de **112,0 cm**.

**Diagnostic** : reproduit la conversion réelle `.doc → .docx` (LibreOffice) puis l'extraction **mammoth**
(exactement ce que voit `bilan-parser.ts`). Le texte contient deux fois « Hanche » :
- en-tête anthropométrique « **Ratio Taille/Hanche** », suivi de la taille « **5' 9"** 175,0 cm » ;
- section « **Circonférences** » plus bas : « Hanche » → « **112,0** ».

Le regex `/Hanche\s*[\n\r]+\s*(\d+…)/` accrochait le **premier** match (l'en-tête) et captait le « 5 » de « 5' 9" ».

**Correctif** : lookbehind `(?<![/\wÀ-ÿ])Hanche…` → ignore « …/Hanche » et vise le libellé isolé de la
section Circonférences. Appliqué aux **deux** extractions (courante l.180 + historique l.258).

**Vérif** : test de non-régression `electron/lib/bilan-parser.test.ts` (fixture inline reproduisant le piège) +
intégration sur le vrai `.docx` → `tour_hanche = 112`, taille 175 et tour de taille 99 inchangés.
Suite complète 295 pass, tsc node+web propres, build OK.

**Nettoyage** : suppression de `test-files/test-parser.mjs` (script de debug jetable : lisait un fichier
disparu → `node --test` échouait, et embarquait une copie périmée/buggée du parser). `tsconfig.node.json`
exclut désormais `electron/**/*.test.ts` du build.

## v0.8.5 — % de gras : « Très bien » vs « En santé » → une seule description

**Symptôme** (Marie / Nicholas) : dans le rapport, le même % de gras (23,1 % homme) était décrit
« **Très bien** » sur la carte composite et « **En santé** » dans la grille détaillée.

**Cause** : deux systèmes de classification pour le même champ — percentile ACSM (À améliorer…Excellent)
vs grille de risque de Marie (Risques potentiels / Optimal / En santé / …). Le document HTML interactif
excluait déjà `bodyFat` du percentile (grille seule) ; le PDF, lui, affichait encore le percentile dans
`CompositeBreakdown` → d'où la divergence PDF vs HTML relevée au départ.

**Décision (utilisateur)** : la **grille de Marie** devient la référence unique du % de gras, **libellé +
score**.

**Implémentation** :
- `bodyFatGridRating(pct, sex)` dans `body-fat-risk.ts` → `{ label, category }` (zone→cote :
  optimal=EXCELLENT, sante=TRES_BIEN, potentiel=BIEN, modere=ACCEPTABLE, eleve=A_AMELIORER).
- `bilan-computed.catFor` : `pourcentage_gras` coté via la grille (impacte composition + composite bodyFat).
- `ReportPage.CompositeBreakdown` : libellé de zone pour le % de gras.
- ADR 0024.

**Vérif** : suite complète **298 pass** (+3), test composition Nicholas inchangé (30,2 % → « Risques
modérés » → ACCEPTABLE = ancien percentile), tsc web+node propres, build OK.

**Hors scope (signalé)** : les cartes composites 0-4 du dashboard (`BilanSynthesisCards`, « % gras
corporel ») gardent le vocabulaire 0-4. Zones extrêmes (trop maigre) : cotation indicative à valider.

## v0.8.6 — Carte dashboard « % gras corporel » alignée sur la grille

Suite de v0.8.5 (demande utilisateur). La carte « % gras corporel » du dashboard montrait encore
« Très bien » (0-4). `computeBilan` expose désormais `bodyFatGridLabel` (libellé de zone de la grille) ;
`CategoryBadge` accepte un prop `label` optionnel (remplace le texte, garde la couleur de la cote) ;
`BilanSynthesisCards` passe `computed.bodyFatGridLabel` pour cette carte → « En santé ». Le % de gras dit
maintenant « En santé » partout (rapport PDF, HTML, dashboard). Suite 298 pass, tsc web+node propres, build OK.

## v0.9.0 — Norme CPAFLA sélectionnable (barèmes musculosquelettiques)

Marie a fourni le Guide du conseiller CPHV 3e éd. (SCPE) — source des barèmes de son ancien logiciel.

**Fait** :
- Tables CPAFLA musculo encodées (`cpafla.ts`) depuis Fig. 7-18 (H) / 7-19 (F) : pushups, situps,
  trunkFlexion, verticalJump, legPower, backEndurance × 6 tranches d'âge × 2 sexes. Helper `band(a,b,tb,e)`
  (borne basse Acceptable→p10, Bien→p25, Très bien→p50, Excellent→p75).
- Repli sur ACSM pour VO2max/IMC/tour de taille (index.getRange + bilan-computed.categorizeRaw) — sinon
  ces scores disparaîtraient sous CPAFLA. % gras = grille de Marie (hors norme).
- `NormsCard` (Paramètres → Bilans) redevient un vrai sélecteur ACSM/CPAFLA, **ACSM par défaut**, CPAFLA
  « En calibration ».
- Tests : spot-checks CPAFLA contre le guide (norms.test.ts) + repli end-to-end (bilan-computed.test.ts).
  Suite 300 pass, tsc web+node propres, build OK. ADR 0025.

**À venir (Marie a le livre complet)** : notes pondérées (Fig. 7-20/7-24) + nomogramme (7-21/7-25) pour la
note combinée musculo et l'indice de santé du dos — diffèrent de la moyenne 0-4 actuelle. À calibrer ensemble.
Force de préhension : pas de champ de bilan → non encodée.

## v0.9.1 — CPAFLA : note combinée musculo + santé du dos (pondérations + nomogramme)

Suite v0.9.0 (barèmes validés par Marie). Implémentation de la note combinée du guide sous norme CPAFLA.

- Nouveau `src/lib/norms/cpafla-combined.ts` : `cpaflaCombine(contribs)` = note pondérée + nomogramme
  (`arrondi-demie-inférieure(obtenue/max×4)`, `Math.ceil(x-0.5)`), validé sur exemples du guide
  (dos 23/28→3, musculo 13/24→2). Poids par sexe (Fig. 7-20 corrigée H/F + 7-24) : musculo ext. bras ×2 (H) /
  flexion ×2 (F) ; dos ext. dos ×2 partout + taille ×2 (F). Préhension + activité physique exclues (choix
  utilisateur → note max réduite).
- `bilan-computed` : si `norms==='cpafla'` (sexe connu), `musculoGlobal` + `backHealth` utilisent cette
  méthode ; sinon ACSM historique inchangé.
- Tests : cpafla-combined.test.ts (nomogramme vs exemples, arrondi -0, exclusions, poids) + intégration
  bilan-computed (H 25 : musculo 3, dos 3). Suite 308 pass, tsc web+node propres, build OK. ADR 0026.

**À valider avec Marie** : nomogramme reproduit par formule (pas transcrit cellule par cellule) — vérifier
sur cas réels vs son logiciel ; écart aux bornes ½ = encoder la grille exacte.

## v0.9.2 — Profil musculo : afficher le barème CPAFLA de la tranche d'âge

Demande de Marie : voir, dans le profil musculosquelettique (dashboard), le barème CPAFLA des gens de
l'âge du client pour comprendre pourquoi il est « Excellent » / « Bien ».

- `MusculoRadar.tsx` : bouton « Barème » (visible seulement si `norms==='cpafla'` + âge/sexe connus, en vue
  barres) → composant `BaremeTable` : 6 tests × 5 catégories pour la tranche d'âge + sexe, bornes reconstruites
  depuis `getCpaflaRange` (p10/p25/p50/p75 = Acceptable/Bien/Très bien/Excellent), case de la cote du client
  surlignée. Préférence mémorisée (localStorage `kinesio.musculo.bareme`).
- `getCpaflaRange` ré-exporté par `src/lib/norms/index.ts`.
- Sous ACSM (percentiles), le bouton n'apparaît pas (barème par intervalles = spécifique CPAFLA).
- Suite 308 pass, tsc web propre, build OK.

## v0.9.3 — Total « Aptitude musculosquelettique globale » dans le profil (dashboard)

Demande de Marie : voir le total de l'aptitude musculosquelettique dans le dashboard. Ajout d'un bandeau en tête
de la carte `MusculoRadar` (« Aptitude musculosquelettique globale » : score /4 + catégorie). DashboardTab passe
`computed.musculoGlobal` (prop `global`). Reflète la note combinée CPAFLA (v0.9.1) sous CPAFLA, la moyenne sous
ACSM. Suite 308 pass, tsc web propre, lint propre, build OK.

## v0.9.4 — Dashboard : termes de l'ancien logiciel pour les cartes composites

Demande de Marie (parité avec son ancien logiciel) : renommer les cartes composites du dashboard.
`DashboardTab` : Cœur et endurance → « Aptitude aérobie » ; Santé du dos → « Indice de santé du dos » ;
Force musculaire → « Aptitude musculosquelettique globale » (Composition corporelle inchangé). Entête de
section aérobie alignée aussi. Renommage d'affichage seulement, aucun calcul touché. tsc web propre, build OK.

## v0.9.5 — Anneau du score global sur l'échelle 0-4

Observation confirmée par Marie : l'anneau central `ScoreDonut` affichait « / 5 » alors que tout le reste
(composites, ancien logiciel) est en 0-4. Corrigé : ratio de remplissage `shown/4`, libellé « / 4 », aria
« sur 4 ». Seul endroit concerné (grep dashboard). tsc web propre, build OK.

## v0.9.6 — Composition corporelle : IMC + tour de taille discrets

Demande de Marie : dans « Composition corporelle / Votre silhouette » (dashboard), ne pas donner beaucoup
d'importance à l'IMC et au tour de taille. Remplacé les 2 grosses cartes `StatCardXL` (catégorie/barre/historique)
par une ligne d'info discrète (libellé + valeur grisés). Le % de gras reste la mesure mise en avant. Suppression
des consts ImcCard/TailleCard devenues inutilisées. tsc web + lint propres, build OK.

## v0.9.7 — Composition corporelle : taille + poids ajoutés, texte plus gros

Suite v0.9.6. Ajout de Taille et Poids à la ligne d'info (ordre Taille · Poids · IMC · Tour de taille, comme
l'anthropométrie de l'ancien logiciel). Poids en double unité (`dualWeight`). Texte agrandi (text-sm → text-base,
valeurs en marine plein) mais toujours simple info (pas de cote/barre). tsc web + lint propres, build OK.

## v0.9.8 — Taille + poids en métrique ET impérial (comme l'ancien bilan)

Demande de Marie : afficher les deux unités comme l'ancien bilan. Taille → « 5' 9¼" · 176 cm » (helper
`cmToFeetInches` dans units.ts, arrondi au ¼ po) ; Poids → « 202,4 lb · 91,8 kg ». IMC/tour de taille en unité
unique (comme l'ancien). Test units.test.ts (176→5'9¼", reports, null). Suite verte, tsc web propre, build OK.

## v0.9.9 — Score de composition corporelle selon CPAFLA

Marie : le chiffre de composition divergeait (2,0 vs 4,0 de l'ancien logiciel). Implémentation de la méthode du
guide (Fig. 7-4/7-5, formule p. 7-18) sous norme CPAFLA.

- Nouveau `src/lib/norms/cpafla-composition.ts` : IMC + CT + S5PC (5 plis), points CT/plis dépendants de la plage
  d'IMC, formule `arrondi[(B×1,5 + C)/2,5]` ; combinaisons IMC+CT / IMC+S5PC / CT seule / IMC seul. Pas d'âge.
  Validé sur l'exemple du guide (femme IMC 25,8/CT 91/S5PC 116,6 → 1 Acceptable).
- `bilan-computed` : composition norm-aware (CPAFLA → cpaflaComposition ; ACSM → moyenne historique).
- 5ᵉ pli **mollet** ajouté au formulaire (mode auto : 5 plis → formule complète ; sinon repli IMC+CT). Le % de
  gras n'entre plus dans le score composition sous CPAFLA (garde sa grille pour l'affichage).
- Tests : cpafla-composition.test.ts (8) + intégration bilan-computed (exemple guide → 1 ; sans mollet → B ;
  Nicholas CPAFLA → 2). Suite 320 pass, tsc web+node propres, build OK. ADR 0027.
